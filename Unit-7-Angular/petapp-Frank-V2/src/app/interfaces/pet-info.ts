//interface describes the properties of an object
//used so any features needing the data has the same description
export interface PetInfo {
    name : string,
    type : string,
    gender : string,
}
